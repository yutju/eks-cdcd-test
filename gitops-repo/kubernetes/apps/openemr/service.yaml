apiVersion: v1
kind: Service
metadata:
  name: openemr-service
  namespace: his-emr
spec:
  type: NodePort
  selector:
    app: openemr
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
